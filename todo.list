处理所有 obj=obj obj+obj || && <这些对于 ！iscomplete的部分
对于obj=obj，可能需要扩展classScope的回填，用于判断两边类型是否一致



class myclass{
    function f():int{
        var a:myclass;
        d=e;//这种回填检查还没有做
        return a.d;//这种已经做了
    };
    var d:int;
    var e:int;
}
在myclass还未封闭的时候,使用了a.d
生成如下代码
tmp_0=class.[-1] //在myclass完成的时候回填为0
tmp_1=tmp_0.[-1] //在myclass类型回填的时候回填为0,类型回填还没做


class myclass{    
    function f():int{
        var a:myclass;
        return a.d.v;
    };
    var d:B;
}
class B{
    var v:int;
}
在myclass还未封闭的时候,使用了a.d
生成如下代码
tmp_0=class.[-1] //在myclass完成的时候回填为0
tmp_1=tmp_0.[-1] //在myclass类型回填的时候回填为0,还没做
tmp_2=tmp_0.[-1] //在B类型回填的时候回填为0